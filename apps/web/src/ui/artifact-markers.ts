/**
 * Stable, non-visible markers for UI capabilities whose rendered controls are
 * data-dependent. The release candidate gate uses these to distinguish a
 * current OpenSpell artifact from an older deployment with the same heading.
 */
export const RECOMMENDATION_REVIEW_ARTIFACT = 'recommendation-review-filtered-actions-v1';
export const OPENSPELL_BRAND_MARK_ARTIFACT = 'official-openspell-brand-mark-v1';
