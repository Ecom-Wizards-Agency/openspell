/**
 * @wizard-ads/mcp (owned by WP-09).
 *
 * Read-only MCP server (Streamable HTTP), API-key auth per org, every call
 * written to audit_log. Lets Claude agents QA the data layer during crosscheck.
 *
 * Scaffold stub: WP-09 replaces this file. WP-00 owns only the manifest,
 * the tsconfig and the dependency wiring.
 */
export const PACKAGE_NAME = '@wizard-ads/mcp' as const;
