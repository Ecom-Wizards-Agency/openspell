/**
 * @wizard-ads/db (owned by WP-01).
 *
 * Drizzle schema, typed queries and RLS test helpers over the Supabase Postgres.
 * Owns nothing above it: callers pass contracts in and get contracts back.
 *
 * Scaffold stub: WP-01 replaces this file. WP-00 owns only the manifest,
 * the tsconfig and the dependency wiring.
 */
export const PACKAGE_NAME = '@wizard-ads/db' as const;
