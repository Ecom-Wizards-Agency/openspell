{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // This package reads files, resolves URLs and talks to a process
    // environment: the seed scripts and the test harness are Node programs.
    "types": ["node"]
  },
  "include": [
    "src/**/*.ts",
    "../../supabase/seed/**/*.ts"
  ]
}
