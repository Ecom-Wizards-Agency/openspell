/**
 * @wizard-ads/ads-api (owned by WP-02).
 *
 * Amazon Ads API client: LWA refresh, profiles, entities, Exports, Reporting v3,
 * per-region token buckets and 429 backoff. A pure client. It never touches the
 * database, and nothing outside apps/worker may import it.
 *
 * Scaffold stub: WP-02 replaces this file. WP-00 owns only the manifest,
 * the tsconfig and the dependency wiring.
 */
export const PACKAGE_NAME = '@wizard-ads/ads-api' as const;
