/**
 * @wizard-ads/strategy (owned by WP-05).
 *
 * Tenant config resolution: defaults, then goal lens, then tenant and profile
 * overrides. Ships a TEMPLATE only. Doctrine values are database rows on the
 * operator side, never literals in this repo.
 *
 * Scaffold stub: WP-05 replaces this file. WP-00 owns only the manifest,
 * the tsconfig and the dependency wiring.
 */
export const PACKAGE_NAME = '@wizard-ads/strategy' as const;
