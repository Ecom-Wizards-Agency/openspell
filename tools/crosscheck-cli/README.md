# crosscheck-cli

Owned by **WP-10**. Compares our facts against AdLabs exports per profile and day,
and produces the verdict table that gates every write in this product.

Not scaffolded here beyond the directory: WP-10 owns the package manifest and the
CLI itself, and the crosscheck job handler is handed to WP-03.
