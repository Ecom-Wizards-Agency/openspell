# recon

Owned by **WP-11**. Output specs from the AdLabs UI walkthrough (the operator logs in;
the agent records what each screen does). **Specs only, no code.**

Screenshots and any capture that identifies an account stay out of this directory.
This repo is public.

**Start at [`00-INDEX.md`](00-INDEX.md)** — file map, nav-map coverage table, read-only session
log, and the consolidated clone / skip / beat verdicts. Then read
[`BLOCKED.md`](BLOCKED.md), which records why the UI walkthrough half did not run.
