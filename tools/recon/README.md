# recon

Owned by **WP-11**. Output specs from the AdLabs UI walkthrough (the operator logs in;
the agent records what each screen does). **Specs only, no code.**

Any capture that identifies an account stays out of this directory. **This repo is public.**
`screenshots/` holds 13 redacted captures; client-identifying screens are described in prose
instead.

**Start at [`00-INDEX.md`](00-INDEX.md)** — file map, screenshot index, nav-map coverage table,
session log, and the consolidated clone / skip / beat verdicts. Its opening section lists **the
four headline conclusions that session 3 overturned**; read that before trusting any older claim
in these files.

[`BLOCKED.md`](BLOCKED.md) is the session log: why sessions 1–2 could not reach a browser, and
the session-3 resolution with the priority-list outcome.

Evidence is tagged in place — **`UI-verified`** beats `MCP` beats `VID` beats `INF`.
